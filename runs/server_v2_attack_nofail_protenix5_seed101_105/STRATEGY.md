# Strategy Record

Run ID: `server_v2_attack_nofail_protenix5_seed101_105`

Strategy name: `yang_coverage_stoich_low_complexity_large_fallback_v1_server_attack_protenix5`

Parent run: `server_v2_protenix_yang_coverage_stoich_low_complexity_large_fallback_seed101`

Benchmark: `casp16_server_protein_v2_aliasfix`

## Hypothesis

The v2 no-over-token stack is the first local server-target input set that
combines alias-fixed coverage, token-safe stoichiometry, construct cleanup, and
large-target fallback without sending any job above Protenix's 2560-token
guard. A five-candidate attack run can test whether reference-free confidence
selection gives useful lift once the input no longer has guaranteed Protenix
failures.

This is an attack-tier run, not a `dev_fixed` ablation. Compare it only against
other attack rows and official server groups with the candidate budget shown.

## Changed Knobs

- Base input:
  `strategies/yang_coverage_stoich_low_complexity_large_fallback_v1/casp16_server_protein_v2_aliasfix/inputs.json`
- Candidate budget: five Protenix seeds, `101..105`.
- Selected model policy: `protenix_confidence_v1`.
- All 163 jobs are at or below the Protenix 2560-token limit.

## Fixed Budget

- backend: `protenix`
- model: `protenix-v2`
- seeds: `101,102,103,104,105`
- sample per seed: `1`
- total candidates per target: `5`
- selected model policy: `protenix_confidence_v1`
- MSA/templates/default params/cache/fusion/TF32: enabled

## Commands

```bash
./casp16 run-spec \
  --run-id server_v2_attack_nofail_protenix5_seed101_105 \
  --benchmark casp16_server_protein_v2_aliasfix \
  --input-json strategies/yang_coverage_stoich_low_complexity_large_fallback_v1/casp16_server_protein_v2_aliasfix/inputs.json \
  --input-manifest strategies/yang_coverage_stoich_low_complexity_large_fallback_v1/casp16_server_protein_v2_aliasfix/manifest.tsv \
  --strategy yang_coverage_stoich_low_complexity_large_fallback_v1_server_attack_protenix5 \
  --seeds 101,102,103,104,105 \
  --sample 1 \
  --selected-model-policy protenix_confidence_v1 \
  --use-msa --use-template --use-default-params \
  --enable-cache --enable-fusion
```

## No Oracle Use

- Native/reference structures were not read during input generation or model
  selection.
- Official score tables were not used for target-specific parameter choices.
- Previous target-level local scores were not used to select edited targets.
- Model selection may read only confidence JSON files generated by this run.

## Launch Gate

Run after the three v2 `dev_fixed` rows are scored:

- `server_v2_protenix_yang_coverage_stoich_seed101`
- `server_v2_protenix_yang_coverage_stoich_low_complexity_seed101`
- `server_v2_protenix_yang_coverage_stoich_low_complexity_large_fallback_seed101`

Keep the row unranked/partial until candidates for all five declared seeds are
present.
